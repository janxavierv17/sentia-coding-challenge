# frozen_string_literal: true

# Seed the database with Star Wars character data from users.csv
# Seed the database with Star Wars character data from users.csv
# csv_path = Rails.root.join("users.csv")

# if csv_path.exist?
#   puts "Cleaning existing data..."
#   Person.destroy_all

#   puts "Importing characters from #{csv_path}..."
#   result = CsvImporter.new(csv_path).call
#   puts "Import complete: #{result.imported_count} imported, #{result.skipped_count} skipped."
#   puts "Errors: #{result.errors.join(', ')}" if result.errors.any?
# else
#   puts "No users.csv found at #{csv_path}. Skipping seed."
# end
